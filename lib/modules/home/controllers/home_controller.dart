import 'package:get/get.dart';

class HomeController extends GetxController {
  // Your network code is gone!
  // Keep only the logic needed for the Home Screen here.

  @override
  void onInit() {
    super.onInit();
  }
}